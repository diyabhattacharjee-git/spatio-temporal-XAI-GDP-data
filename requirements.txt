# =============================================================================
# EconXAI Dashboard — requirements.txt
# Tested on Python 3.10 / 3.11 / 3.12
# =============================================================================

# --- Core web framework ---
streamlit>=1.35.0,<2.0.0

# --- Data science ---
pandas>=2.0.0,<3.1.0
numpy>=1.26.0,<3.0.0
scikit-learn>=1.4.0,<2.0.0

# --- ML & Explainability ---
xgboost>=2.0.0,<3.0.0
shap>=0.45.0,<1.0.0

# --- Visualisation ---
plotly>=5.22.0,<6.0.0

# --- HTTP ---
requests>=2.31.0,<3.0.0

# --- Finance data (optional — graceful fallback to synthetic if missing) ---
yfinance>=0.2.40,<1.0.0

# --- Export helpers (optional — graceful fallback if missing) ---
xlsxwriter>=3.2.0,<4.0.0
kaleido>=0.2.1,<1.0.0

# --- Indirect deps pinned to avoid CI/CD resolver surprises ---
scipy>=1.13.0,<2.0.0